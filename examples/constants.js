// Local development environment port and URL constants
// Change ports here and all example apps and e2e tests will pick them up.
/** Port number for the local Transcodes backend server. */
export var DEV_BACKEND_PORT = 3500;
/** Port number for the local example app dev server. */
export var DEV_EXAMPLE_PORT = 9999;
/** Full URL for the local Transcodes backend server. */
export var DEV_BACKEND_URL = "http://localhost:".concat(DEV_BACKEND_PORT);
/** Full URL for the local example app dev server. */
export var DEV_EXAMPLE_URL = "http://localhost:".concat(DEV_EXAMPLE_PORT);
// Project configuration for e2e tests
export var TEST_PROJECT_ID = 'ca47abf37d3ed05be83ef4b1';
