---
"@bigstrider/transcodes-sdk": minor
---

- Add `baseUrl` option to `init()`: allows loading the toolkit script from a local backend instead of the CDN
- Centralize example app port/URL constants into `examples/constants.ts`
- Improve e2e tests to verify modal opening (DOM appearance)
